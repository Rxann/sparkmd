/// <reference types="node" />
import type { WatchMode } from 'esbuild';
import { Worker } from 'worker_threads';
import type { Logger, TypecheckPluginOptions } from './types';
export interface TypecheckRunnerOptions extends Omit<TypecheckPluginOptions, 'watch'> {
    absWorkingDir: string;
    watch?: boolean | WatchMode;
}
export declare class TypecheckRunner {
    configPath: string;
    logger: Logger;
    worker: Worker;
    constructor({ absWorkingDir: basedir, logger, omitStartLog, watch, ...options }: TypecheckRunnerOptions);
    start(): void;
}
//# sourceMappingURL=typecheck-runner.d.ts.map